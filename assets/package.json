{
  "name": "beam_chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phoenix LiveView assets for BEAM Chat",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "node --test test/hooks/*.test.js"
  },
  "dependencies": {
    "livekit-client": "^2.5.0"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
